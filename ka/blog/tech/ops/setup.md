# Სერვერის Ინიციალიზაცია (Გამოიყენეთ BTRFS Დანაყოფი, Snapper Დაგეგმილი Სნეპშოტი)

## Კონტაბო Შესყიდვის Სერვერი

contabo არის იაფი VPS პროვაიდერი გერმანიაში ხანგრძლივი ისტორიით (20 წელზე მეტი) და ბევრი მომხმარებელი (200000-ზე მეტი), ასე რომ არ არის საჭირო გაქცევაზე ფიქრი.

VPS გახსოვდეთ [contabo.com](//contabo.com) რომ აირჩიოთ ევრო ფასი, რომელიც უფრო იაფია.

<img alt="" src="https://i-01.eu.org/1718285782.avif">

კონტაბოსთვის არის ერთჯერადი ინსტალაციის საფასური.
თუმცა, ყოველთვიურად არის რამდენიმე დღე, სადაც ინსტალაციის საფასური უფასოა, უბრალოდ დარეგისტრირდით და ყურადღება მიაქციეთ სარეკლამო წერილებს.

ევროპაში თავისუფალი ფართის გადასახადის გარდა, სხვა რეგიონებში ფასების ზრდა განსხვავებულია.

მე ავირჩიე ევროპული სერვერი, ყოველ შემთხვევაში, Cloudflare-ის გამოყენების შემდეგ, სერვერებს შორის დიდი განსხვავება არ არის.

საბოლოო კონფიგურაცია ასეთია, 16 GB მეხსიერება, 6 CPU, 200 GB NVMe, 9.50 ევრო თვეში, 74.43 RMB-ის ექვივალენტი.

<img alt="" src="https://i-01.eu.org/1718286008.avif">

**ეს ფასი არის Alibaba Cloud-ისა და Amazon-ის ფასის თითქმის მეათედი** .

<img alt="" src="https://i-01.eu.org/1718356956.avif">

მე [Yet-Another-Bench-Script](https://github.com/masonr/yet-another-bench-script) შესრულების და პარამეტრების გასაშვებად:

```
curl -sL yabs.sh | bash -s -- -s https://www.vpsbenchmarks.com/yabs/upload
```

ჩვენება CPU `AMD EPYC 9224 24-Core Processor` ერთბირთვიანი შესრულების ქულა 1285
, მრავალბირთვიანი ქულა 4277 იხილეთ მეტი შედეგი [vpsbenchmarks.com](https://www.vpsbenchmarks.com/yabs/contabo-6c-16gb-3fb7d8)

მე ვიყიდე სამი სერვერი ერთდროულად და გავააქტიურე ისინი ერთდროულად.

მისი სერვერები დროდადრო გათიშულია, რაც უზრუნველყოფს ვებსაიტის ხელმისაწვდომობას მაღალი ხელმისაწვდომობით.

ახალმა მომხმარებლებმა შეძენისას უნდა წარმოადგინონ პირადობის მოწმობები ნამდვილი სახელის ავთენტიფიკაციისთვის.

## Პაროლის Გარეშე Შესვლის Კონფიგურაცია

რამდენიმე წუთის შემდეგ ყველა სერვერი გააქტიურდა.

უპირველეს ყოვლისა, დაარედაქტიროთ ადგილობრივი აპარატი `~/.ssh/config` -სთვის IP შემდეგ გამოიყენეთ `ssh-copy-id root@c2` , პაროლის გარეშე შესვლის კონფიგურაციისთვის.

<img alt="" src="https://i-01.eu.org/1718287198.avif">

თქვენ შეგიძლიათ შეცვალოთ ეს [ssh-copy-id.sh](https://github.com/i18n-ops/ubuntu/blob/main/ssh-copy-id.sh) , რათა დაასრულოთ ინიციალიზაციის კონფიგურაცია უფრო სწრაფად.

პაროლის გარეშე კონფიგურაციის დასრულების შემდეგ, შეგიძლიათ [pdsh](https://github.com/chaos/pdsh) იგი სერიულად, რაც მოსახერხებელია.

პირველი არის ხელით ჩართოთ ipv6 და შეამოწმოთ

```bash
pdsh -w 'c0 c1 c2' 'bash -i -c enable_ipv6'
pdsh -w 'c0 c1 c2' 'curl -s 6.ipw.cn;echo'
```

<img alt="" src="https://i-01.eu.org/1718287969.avif">

შემდეგ განაახლეთ სისტემა ubuntu 24-ზე ერთი დაწკაპუნებით და დააინსტალირეთ ხშირად გამოყენებული პროგრამული უზრუნველყოფა.

```bash
pdsh -w 'c0 c1 c2' 'curl -s https://raw.githubusercontent.com/i18n-ops/ubuntu/main/boot.sh | CN=1 bash'
```

CN=1 ნიშნავს ენის შეცვლას ჩინურზე და დროის ზონაში პეკინზე.

ნაგულისხმევად დაინსტალირებული პროგრამული [i18n-ops/ubuntu](https://github.com/i18n-ops/ubuntu) კონფიგურირებულია ჩემი პირადი გამოყენების ჩვევების მიხედვით, როგორიცაა [mise](https://github.com/jdx/mise) [direnv](https://direnv.net/) და ა.შ.

ამ ნაბიჯს დიდი დრო სჭირდება ნახევარი საათის ლოდინის შემდეგ, ჩვეულებრივ გამოყენებული პროგრამული უზრუნველყოფის განახლება და ინსტალაცია წარმატებული იქნება. ამის დადასტურება შეგიძლიათ შემდეგი ბრძანებით:

```bash
pdsh -w 'c0 c1 c2' 'cat /etc/issue'
```

<img alt="" src="https://i-01.eu.org/1718290346.avif">

## mosh Აჩქარებს ssh

ქსელი `ssh` ჩინეთში არ არის სტაბილური [mosh](//mosh.org)

შექმენით `c0` სკრიპტი ლოკალური `~/.bin` დირექტორიაში (ძირითადად დამატებულია გარემოს ცვლადის `PATH` ) შემდეგნაირად:

```bash
#!/usr/bin/env bash

if [ -z "$1" ]; then
arg=/root/.tmux_default
else
arg=${@:1:$#}
fi

vps=$(basename $0)
set -ex
mosh $vps -p 60000:60009 $arg
```

შექმნის შემდეგ, მიანიჭეთ შესრულებადი ნებართვები და soft-link `c1` `c2` .

```bash
chmod +x c0
ln -s c0 c1
ln -s c0 c2
```

მომავალში შეგიძლიათ პირდაპირ შეიყვანოთ `c0` `c1` და შეგიძლიათ გამოიყენოთ `mosh` მაღალსიჩქარიანი პირდაპირი კავშირი და შეიყვანოთ [tmux](https://www.ruanyifeng.com/blog/2019/10/tmux.html)

## Გადაიყვანეთ Დანაყოფის Ფორმატი Ext4-Დან Btrfs-Ში

[btrfs](https://wikipedia.org/wiki/Btrfs) დისკის კადრების გადაღება მარტივი სარეზერვო ასლისთვის.

ჯერ შედით სამაშველო სისტემაში

<img alt="" src="https://i-01.eu.org/1718300448.avif">

აირჩიეთ Debian Rescue

<img alt="" src="https://i-01.eu.org/1718300753.avif">

დაელოდეთ სერვერის გადატვირთვას

თითის ანაბეჭდის ცვლილებები მოხდება `ssh-keygen -R ip` როცა ssh

შემდეგ გაუშვით შემდეგი სკრიპტი დანაყოფის გადასაყვანად

```bash
nohup bash -c "bash <(curl -s https://raw.githubusercontent.com/i18n-ops/ops/main/contabo/ext4_btrfs.sh) >/tmp/nohup.out 2>&1 && reboot" &
sleep 1 && tail -f /tmp/nohup.out
```

სკრიპტი ავტომატურად გადაიტვირთება სისტემას კონვერტაციის წარმატებით დასრულების შემდეგ.

### Ნახეთ btrfs Შეკუმშვის Კოეფიციენტი

ის ავტომატურად შეკუმშავს შეკუმშვის კოეფიციენტის შემოწმების ბრძანებას:

```bash
if ! command -v compsize &>/dev/null; then
apt install -y  btrfs-compsize
fi
compsize -x /
```

### Ხელით Შეკუმშვა

თუ პერფექციონისტი ხართ, ასევე შეგიძლიათ ხელით შეასრულოთ დისკის სრული შეკუმშვა:

```bash
btrfs filesystem defragment -r -czstd /
```

როგორც ქვემოთ მოყვანილ სურათზეა ნაჩვენები, ხედავთ, რომ ახალ სისტემაში ხელით შეკუმშვის შემდეგ შეკუმშვა მოხდა თითქმის 100 მბ-ით მეტი.

<img alt="" src="https://i-01.eu.org/2023/12/8NWlr4c.webp">

### Შექმენით Btrfs Სნეპშოტის Სარეზერვო Ასლი Snapper-Ით

[snapper](https://manual.siduction.org/sys-admin-btrfs-snapper_en.html) არის საუკეთესო პარტნიორი Btrfs სარეზერვო ასლისთვის.

ინიციალიზაციის სკრიპტი შექმნის `~/ubuntu` , უბრალოდ გაუშვით შემდეგი ბრძანება:

```
~/ubuntu/snapper/init.sh
```

შევხვდებით სცენარის შინაარსში [github.com/i18n-ops/ubuntu/blob/main/snapper/init.sh](https://github.com/i18n-ops/ubuntu/blob/main/snapper/init.sh)

გამოიყენეთ `snapper list-configs` იმ ქვეტომების სანახავად, რომლებისთვისაც დაყენებულია კადრები.

<img alt="" src="https://i-01.eu.org/1718302689.avif">

გამოიყენეთ `snapper -c mnt list`

`/etc/snapper/configs/mnt` შეგიძლიათ ნახოთ ამ ქვეტომის კონკრეტული სარეზერვო პარამეტრები

მეტი მითითება:

[მარტივად ითამაშეთ Btrfs სნეპშოტის ფუნქციით snapper](https://zhuanlan.zhihu.com/p/31082518)

## Კონტაბო Სერვერის Სნეპშოტი

contabo სერვერის სნეპშოტების ფუნქციას, მაგრამ არ [github action](https://github.com/i18n-cron/cron/actions/workflows/contabo.snapshot.yml) მხარს დაგეგმილ სნეპშოტებს.

<img alt="" src="https://i-01.eu.org/1718304206.avif">

სერვერის სნეპშოტის აღსადგენად, ჯერ დააჭირეთ Crontol-ს მართვის მენიუში

<img alt="" src="https://i-01.eu.org/1718304275.avif">

შემდეგ, დააწკაპუნეთ Snapshots აღსადგენად.

<img alt="" src="https://i-01.eu.org/1718304063.avif">

თუმცა, ამ სნეპშოტის დეტალურობა ძალიან დიდია, როგორც კი აღდგება, ახლად დამატებული მონაცემები მთლიანად დაიკარგება, ამიტომ გთხოვთ გამოიყენოთ იგი სიფრთხილით.

როგორც წესი, უფრო მოსახერხებელია btrfs სნეპშოტების გამოყენება.

## Სამაშველო Სისტემის Დეტალები

შედით სამაშველო სისტემაში მას შემდეგ 22 რაც პორტი იქნება [tcping](https://github.com/paradiseduo/tcping) წვდომა, თქვენ ვერ შეძლებთ მასზე წვდომას დაყენებული პაროლით მხოლოდ 2-3 წუთით და ყველაფერი კარგად იქნება.

```
tcping ip 22 -c 9999
```

პორტის ხელმისაწვდომობის მონიტორინგი შესაძლებელია.

### Როგორ Შეხვიდეთ Მყარ Დისკზე Სამაშველოში & Შემდეგ

```
mkdir -p /mnt/os
mount  /dev/sda3 /mnt/os -t btrfs -o "defaults,ssd,discard,noatime,compress=zstd:3,space_cache=v2"
mount --bind /dev /mnt/os/dev
mount --bind /proc /mnt/os/proc
mount --bind /sys /mnt/os/sys
mount --bind /run /mnt/os/run
chroot /mnt/os zsh -c "mount -a && dbus-daemon --system 2>/dev/null; exec zsh"
```

თქვენ შეგიძლიათ ნახოთ სნეპშოტი, მაგალითად

```
snapper -c etc list
```

Snapshot ფაილის ცვლილებების ნახვა

```
snapper -c etc status 5..6
```

იხილეთ კონკრეტული განსხვავებული ტექსტი

```
snapper -c etc diff 3..5
```

სნეპშოტის აღდგენა: მაგალითად, გააუქმეთ ყველა ცვლილება სნეპშოტის 5-ის შემდეგ

```
snapper -c etc undochange 1..0
snapper -c disk undochange 1..0
```

### Ჟურნალის Მდგრადობის Ჩართვა

სამაშველო სისტემაში ჟურნალის მდგრადობის ჩართვა მოითხოვს, რომ ხელით შექმნათ შესაბამისი დირექტორია და შეასრულოთ რამდენიმე დამატებითი ნაბიჯი, რადგან შეიძლება არ გქონდეთ გაშვებული systemd სერვისი. აქ არის ნაბიჯები, რომლებიც შეგიძლიათ სცადოთ:

ჯერ chroot, მერე

```
mkdir -p /var/log/journal
chown root:systemd-journal /var/log/journal
chmod 2755 /var/log/journal
sed -i 's/^#\?\s*Storage=.*/Storage=persistent/' /etc/systemd/journald.conf
```

გადატვირთეთ თქვენს Ubuntu სისტემაში: ზემოაღნიშნული ცვლილებების განხორციელების შემდეგ, თქვენ უნდა გადატვირთოთ თქვენი მანქანა ჩვეულებრივ და თქვენს Ubuntu სისტემაში სამაშველო სისტემის ნაცვლად.

ამ გზით systemd-journald დაიწყებს ახალი კონფიგურაციით და დაიწყებს ჟურნალების მუდმივ შენახვას მყარ დისკზე.

გაითვალისწინეთ, რომ ზემოაღნიშნული ოპერაციები შესრულებულია სამაშველო სისტემაზე, ამიტომ ეს ცვლილებები ძალაში შევა მხოლოდ სისტემის გადატვირთვის შემდეგ და აღარ იქნება სამაშველო რეჟიმში.

ეს ნიშნავს, რომ თქვენ დაუყოვნებლივ ვერ ნახავთ რაიმე ცვლილებას არსებულ ჟურნალებში, რომლებიც მუდმივი გახდება, მაგრამ ამის შემდეგ გენერირებული ყველა ჟურნალი შენარჩუნდება.

ამ გზით შეგიძლიათ ნახოთ ssh სერვისის ჟურნალები

```
journalctl --directory=/var/log/journal  _SYSTEMD_UNIT=ssh.service
```

## Შენიშვნები Რთულ Და Რთულ Დაავადებებზე

### Მოაგვარეთ Უცნაური Პრობლემა: Სერვერს Არ Შეუძლია ssh

ადრე გამოვიყენე github მოქმედება ავტომატური შედგენისა და განლაგების სკრიპტის შესაქმნელად, მაგრამ აღმოვაჩინე, რომ განლაგების სერვისი არ შეიძლება იყოს ssh.

სამაშველო სისტემაში შესვლა და ჟურნალების დათვალიერება ნებართვის საკითხია:

`Missing privilege separation directory: /run/sshd`

თქვენ შეგიძლიათ შექმნათ ეს დირექტორია შემდეგი ბრძანებით

```
chown root:root /
systemd-tmpfiles --create
```

განმეორებითი გამოკვლევების შემდეგ, ეს იმიტომ github action რომ მომხმარებელი, რომელმაც ატვირთა tar არის `1001` `uid`

შეფუთვა უნდა განხორციელდეს პარამეტრების გარეშე tar `--no-same-owner` ის დეკომპრესირდება `uid` -ად `1001` მომხმარებლისთვის

შედეგად, rsync შეცვლის root დირექტორიას მომხმარებელთა ჯგუფს და შემდეგ გადატვირთვის შემდეგ ვერ შეძლებთ ssh-ში შესვლას.

შეცვალეთ დეკომპრესიის სკრიპტი შემდეგნაირად:

```bash
tar --no-same-owner -xvf $TZT
rm $TZT

if [ -f "init.sh" ]; then
  ./init.sh
  rm init.sh
fi

rsync --remove-source-files -av . /
```

