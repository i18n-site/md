# Колонтитул

Взявши як приклад демонстраційний проект, файл `.i18n/htm/foot.pug` у каталозі `md` визначає нижній колонтитул веб-сайту.

![](https://p.3ti.site/1721286077.avif)

[`pug`](https://pugjs.org) — це мова шаблонів для генерації `HTML`.

[➔ Натисніть тут, щоб вивчити граматику pug](https://pugjs.org)

**Не додавайте `css` і `js` до `foot.pug`**, інакше виникнуть помилки.

Будь ласка, зверніться до наступного: стилі записуються у відповідні `.css`, а взаємодія реалізується через створення веб-компонентів.

## Стилі нижнього колонтитула

У демонстраційному проекті є три файли `css` у директорії `md/.i18n/htm`.

* `foot.css`: стилі нижнього колонтитула
* `import.css`: базовий стиль для всього сайту `i18n.site`
* `conf.css`: іконки та шрифти для нижнього колонтитула

### Іконки шрифтів

Іконки нижнього колонтитула створюються за допомогою шрифту з назвою `F`, створеного на iconfont.cn ([англійська версія](https://www.iconfont.cn/?lang=en-us) / [китайська версія](https://www.iconfont.cn/?lang=zh)).

Будь ласка, створіть свій власний шрифт іконок за потреби та замініть наступну конфігурацію у `conf.css`:

```
@font-face {
  font-family: "F";
  src: url("//at.alicdn.com/t/c/font_4281991_urfar2m2zce.woff2?t=1716188208767") format("woff2");
}
```

## Веб-компоненти

Не додавайте `js` до `foot.pug`. Якщо потрібна взаємодія, створіть власний веб-компонент.

[Веб-компонент](https://www.freecodecamp.org/news/build-your-first-web-component/) можна визначити у файлі `md/.i18n/htm/index.js`, а потім інтегрувати його в `foot.pug`.

Створення веб-компонентів є простим, наприклад,自定义 тег `<x-img>`.

```js
customElements.define(
  'x-img',
  class extends HTMLElement {
    constructor() {
      super();
      var img = document.createElement('img');
      img.src = '//p.3ti.site/i18n.svg';
      img.style = "height:99px;width:99px;";
      this.append(img);
    }
  }
)
```

## Багатомовність нижнього колонтитула

Код у `.i18n/htm/foot.pug` виглядає так:

```
#Ft
  b
    a.site(href="/")
    b ${I18N.C}
```

Тут `${I18N.C}` відповідає запису в `en/i18n.yml`:

```
C: Power By <a class="a" href="https://i18n.site">i18n.site</a>
```

Використовуючи`${I18N.xxx}` подібний спосіб запису, разом з `i18n.yml`, можна досягти багатомовної міжнародизації нижнього колонтитула.

Додавання `class="a"` до посилання запобігає його перетворенню на формат `Markdown`. Дивіться:
 [➔ `YAML`: Як уникнути перетворення посилань у форматі `HTML` на `Markdown`](/i18/qa#H2).