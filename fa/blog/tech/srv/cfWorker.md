
## 绑定 CNAME域名 به نام دامنه محدود به CNAME

پس از绑定 نام دامنه از طریق `SSL/TLS` به نام میزبان سفارشی، نام دامنه CNAME، دسترسی مستقیم باعث بروز خطای `error code: 522` می‌شود!

در این حالت، باید از مسیریابی HTTP به Workers برای绑定 یک مسیر استفاده کنید، همانطور که در شکل زیر نشان داده شده است:

![](https://p.3ti.site/1719625477.avif)
![](https://p.3ti.site/1719625385.avif)