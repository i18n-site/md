# Серверді Инициализациялау (Btrfs Бөлімін, Снаппердің Жоспарланған Суретін Пайдаланыңыз)

## contabo Сатып Алу Сервері

contabo - бұл Германиядағы ұзақ тарихы бар (20 жылдан астам) және көптеген пайдаланушылар (200 000-нан астам) бар арзан VPS провайдері, сондықтан қашып кету туралы алаңдаудың қажеті жоқ.

Сатып [contabo.com](//contabo.com) VPS еуро бағасын таңдауды ұмытпаңыз, ол арзанырақ.

![](https://i-01.eu.org/1718285782.avif)

Контабо үшін бір реттік орнату ақысы бар.
Дегенмен, ай сайын орнату ақысы тегін болатын бірнеше күн бар, жай ғана тіркеліп, жарнамалық хаттарға назар аударыңыз.

Еуропадағы тегін аумақтық алымнан басқа, басқа аймақтарда бағаның көтерілу деңгейі әртүрлі.

Мен еуропалық серверді таңдадым, бәрібір cloudflare қолданғаннан кейін серверлер арасында үлкен айырмашылық жоқ.

Соңғы конфигурация келесідей, 16 ГБ жад, 6CPU, 200 ГБ NVMe, айына 9,50 еуро, 74,43 RMB-ға баламалы.

![](https://i-01.eu.org/1718286008.avif)

**Бұл баға Alibaba Cloud және Amazon бағасының оннан бір бөлігін құрайды** .

![](https://i-01.eu.org/1718356956.avif)

Мен өнімділік пен параметрлерді іске қосу үшін [Yet-Another-Bench-Script](https://github.com/masonr/yet-another-bench-script) :

```
curl -sL yabs.sh | bash -s -- -s https://www.vpsbenchmarks.com/yabs/upload
```

`AMD EPYC 9224 24-Core Processor` CPU көрсетілуде, бір ядролық өнімділік көрсеткіші 1285
, көп ядролық ұпай 4277 Басқа нәтижелерді көру [vpsbenchmarks.com](https://www.vpsbenchmarks.com/yabs/contabo-6c-16gb-3fb7d8)

Мен бір уақытта үш серверді сатып алдым және оларды бір уақытта іске қостым, бұл серверлердің бір компьютер бөлмесінде болуын қамтамасыз етеді, бұл болашақта жоғары қолжетімділікті құруды жеңілдетеді.

Оның серверлері кейде жоғары қолжетімділік арқылы веб-сайттың қол жетімділігін қамтамасыз етіп, ілулі тұрады.

Жаңа пайдаланушылар сатып алу кезінде нақты аты-жөнін растау үшін жеке куәліктерін ұсынуы керек.

## Құпия Сөзсіз Кіруді Конфигурациялаңыз

Бірнеше минуттан кейін барлық серверлер іске қосылды.

Біріншіден, бүркеншік атты орнату үшін жергілікті құрылғының `~/.ssh/config` IP , содан кейін құпия сөзсіз кіруді конфигурациялау үшін `ssh-copy-id root@c2` пайдаланыңыз.

![](https://i-01.eu.org/1718287198.avif)

Баптандыру конфигурациясын тезірек аяқтау үшін осы [ssh-copy-id.sh](https://github.com/i18n-ops/ubuntu/blob/main/ssh-copy-id.sh) өзгертуге болады.

Құпия сөзсіз конфигурациялау аяқталғаннан кейін оны пайдалануға болады, бұл ыңғайлы [pdsh](https://github.com/chaos/pdsh)

Біріншісі - ipv6-ны қолмен қосу және тексеру

```bash
pdsh -w 'c0 c1 c2' 'bash -i -c enable_ipv6'
pdsh -w 'c0 c1 c2' 'curl -s 6.ipw.cn;echo'
```

![](https://i-01.eu.org/1718287969.avif)

Содан кейін бір рет басу арқылы жүйені ubuntu 24 нұсқасына жаңартыңыз және жиі қолданылатын бағдарламалық құралды орнатыңыз.

```bash
pdsh -w 'c0 c1 c2' 'curl -s https://raw.githubusercontent.com/i18n-ops/ubuntu/main/boot.sh | CN=1 bash'
```

CN=1 тілді қытай тіліне және уақыт белдеуін Бейжіңге ауыстыруды білдіреді.

Әдепкі бойынша орнатылған бағдарламалық құрал , [direnv](https://direnv.net/) және т.б. сияқты жеке пайдалану әдеттеріме сәйкес конфигурацияланған [mise](https://github.com/jdx/mise) Қажет болса, [i18n-ops/ubuntu](https://github.com/i18n-ops/ubuntu) қоймасын көшіріп, қажетінше өзгертуге болады.

Бұл қадам көп уақытты алады, жарты сағат күткеннен кейін жиі қолданылатын бағдарламалық құралды жаңарту және орнату сәтті болады. Мұны келесі пәрмен арқылы растауға болады:

```bash
pdsh -w 'c0 c1 c2' 'cat /etc/issue'
```

![](https://i-01.eu.org/1718290346.avif)

## mosh ssh Жылдамдатады

Еуропадан Қытайға дейінгі желі тұрақты емес. `ssh` жылдамдығын пайдалану ұсынылады [mosh](//mosh.org)

Жергілікті `~/.bin` каталогында (негізінен `PATH` орта айнымалысына қосылады) келесідей `c0` сценарий жасаңыз:

```bash
#!/usr/bin/env bash

if [ -z "$1" ]; then
arg=/root/.tmux_default
else
arg=${@:1:$#}
fi

vps=$(basename $0)
set -ex
mosh $vps -p 60000:60009 $arg
```

Жасалғаннан кейін орындалатын рұқсаттарды және `c1` және `c2` жұмсақ сілтемесін беріңіз.

```bash
chmod +x c0
ln -s c0 c1
ln -s c0 c2
```

Болашақта `mosh` жоғары жылдамдықты тікелей қосылымды пайдалану және енгізу үшін `c0` және `c1` сандарын тікелей енгізуге болады [tmux](https://www.ruanyifeng.com/blog/2019/10/tmux.html)

## Бөлім Пішімін Ext4-Тен Btrfs-Ге Түрлендіру

Оңай сақтық көшірме жасау үшін дискінің суретін түсіре [btrfs](https://wikipedia.org/wiki/Btrfs) .

Алдымен құтқару жүйесіне кіріңіз

![](https://i-01.eu.org/1718300448.avif)

Debian Rescue таңдаңыз

![](https://i-01.eu.org/1718300753.avif)

Сервер қайта іске қосылғанша күтіңіз

ssh Саусақ ізі өзгерістері болады, оны тазалау үшін `ssh-keygen -R ip` пайдаланыңыз

Содан кейін бөлімді түрлендіру үшін келесі сценарийді іске қосыңыз

```bash
nohup bash -c "bash <(curl -s https://raw.githubusercontent.com/i18n-ops/ops/main/contabo/ext4_btrfs.sh) >/tmp/nohup.out 2>&1 && reboot" &
sleep 1 && tail -f /tmp/nohup.out
```

Түрлендіру сәтті болғаннан кейін сценарий жүйені автоматты түрде қайта іске қосады.

### btrfs Қысу Коэффициентін Қараңыз

Ол әдепкі бойынша автоматты түрде қысылады. Қысу коэффициентін тексеру пәрмені келесідей:

```bash
if ! command -v compsize &>/dev/null; then
apt install -y  btrfs-compsize
fi
compsize -x /
```

### Қолмен Қысу

Егер сіз перфекционист болсаңыз, дискіні толық қысуды қолмен орындауға болады. Пәрмен келесідей:

```bash
btrfs filesystem defragment -r -czstd /
```

Төмендегі суретте көрсетілгендей, жаңа жүйеде қолмен қысудан кейін 100 МБ-қа жуық қысылғанын көруге болады.

![](https://i-01.eu.org/2023/12/8NWlr4c.webp)

### snapper Көмегімен Btrfs Суретінің Сақтық Көшірмесін Жасаңыз

[snapper](https://manual.siduction.org/sys-admin-btrfs-snapper_en.html) - Btrfs сақтық көшірмесін жасау үшін ең жақсы серіктес.

Баптандыру сценарийі `~/ubuntu` каталогын жасайды, жай ғана келесі пәрменді іске қосыңыз:

```
~/ubuntu/snapper/init.sh
```

Сценарий мазмұнында кездескенше [github.com/i18n-ops/ubuntu/blob/main/snapper/init.sh](https://github.com/i18n-ops/ubuntu/blob/main/snapper/init.sh)

Суреттер орнатылған ішкі томдарды көру үшін `snapper list-configs` пайдаланыңыз.

![](https://i-01.eu.org/1718302689.avif)

Ішкі томның суретін көру үшін `snapper -c mnt list` пайдаланыңыз

`/etc/snapper/configs/mnt` осы ішкі томның арнайы сақтық көшірме параметрлерін көре алады

Қосымша сілтемелер:

[Btrfs суретін түсіру функциясымен оңай ойнаңыз snapper](https://zhuanlan.zhihu.com/p/31082518)

## contabo Серверінің Суреті

contabo Сервердің суретін түсіру функциясын қамтамасыз етеді, [github action](https://github.com/i18n-cron/cron/actions/workflows/contabo.snapshot.yml) жоспарланған суреттерді қолдамайды.

![](https://i-01.eu.org/1718304206.avif)

Сервер суретін қалпына келтіру үшін алдымен басқару мәзірінде Crontol түймесін басыңыз

![](https://i-01.eu.org/1718304275.avif)

Содан кейін қалпына келтіру үшін Snapshots түймесін басыңыз.

![](https://i-01.eu.org/1718304063.avif)

Дегенмен, бұл суреттің түйіршіктілігі тым үлкен, қалпына келтірілгеннен кейін, жаңадан қосылған деректер толығымен жоғалады, сондықтан оны сақтықпен пайдаланыңыз.

Әдетте btrfs суретін пайдалану ыңғайлырақ.

## Құтқару Жүйесінің Мәліметтері

Құтқару жүйесіне кіріңіз 22 Портқа [tcping](https://github.com/paradiseduo/tcping) қолжетімді болғаннан кейін, сіз оған орнатылған құпия сөзбен 2-3 минут күте алмайсыз.

```
tcping ip 22 -c 9999
```

Порттың қолжетімділігін бақылауға болады.

### & Суретін Енгізгеннен Кейін Қатты Дискіге Қалай Қол Жеткізуге Болады

```
mkdir -p /mnt/os
mount  /dev/sda3 /mnt/os -t btrfs -o "defaults,ssd,discard,noatime,compress=zstd:3,space_cache=v2"
mount --bind /dev /mnt/os/dev
mount --bind /proc /mnt/os/proc
mount --bind /sys /mnt/os/sys
mount --bind /run /mnt/os/run
chroot /mnt/os zsh -c "mount -a && dbus-daemon --system 2>/dev/null; exec zsh"
```

Мысалы, суретті көруге болады

```
snapper -c etc list
```

Сурет файлының өзгерістерін қараңыз

```
snapper -c etc status 5..6
```

Арнайы айырмашылық мәтінін қараңыз

```
snapper -c etc diff 3..5
```

Суретті қалпына келтіру: Мысалы, сурет 5-тен кейінгі барлық өзгерістерді болдырмау

```
snapper -c etc undochange 1..0
snapper -c disk undochange 1..0
```

### Журнал Тұрақтылығын Қосыңыз

Құтқару жүйесінде журнал тұрақтылығын қосу сізге сәйкес каталогты қолмен жасауды және кейбір қосымша қадамдарды орындауды талап етеді, себебі сізде жүйелік қызмет іске қосылмауы мүмкін. Мына қадамдарды орындауға болады:

Алдымен chroot, содан кейін

```
mkdir -p /var/log/journal
chown root:systemd-journal /var/log/journal
chmod 2755 /var/log/journal
sed -i 's/^#\?\s*Storage=.*/Storage=persistent/' /etc/systemd/journald.conf
```

Ubuntu жүйесінде қайта жүктеңіз: Жоғарыда көрсетілген өзгертулерді жасағаннан кейін, құрылғыңызды қалыпты жағдайда және құтқару жүйесінің орнына Ubuntu жүйесінде қайта жүктеуіңіз керек.

Осылайша, systemd-journald жаңа конфигурациядан басталады және журналдарды қатты дискіде тұрақты түрде сақтай бастайды.

Жоғарыда көрсетілген әрекеттер құтқару жүйесінде орындалатынын есте сақтаңыз, сондықтан бұл өзгерістер жүйе қайта жүктелмейінше және құтқару режимінде болмайынша күшіне енбейді.

Бұл бұрыннан бар журналдарға тұрақты болатын өзгерістерді бірден көрмейсіз, бірақ одан кейін жасалған барлық журналдар сақталады дегенді білдіреді.

Осылайша сіз ssh қызметінің журналдарын көре аласыз

```
journalctl --directory=/var/log/journal  _SYSTEMD_UNIT=ssh.service
```

## Күрделі Және Күрделі Аурулар Туралы Ескертулер

### Біртүрлі Мәселені Шешіңіз: Сервер ssh Жасай Алмайды

Мен бұрын автоматты жинақтау және орналастыру сценарийін жасау үшін github әрекетін қолдандым, бірақ орналастыру қызметі ssh бола алмайтынын анықтадым.

Құтқару жүйесіне кіру және журналдарды қарау рұқсат мәселесі болып табылады:

`Missing privilege separation directory: /run/sshd`

Бұл каталогты келесі пәрмен арқылы жасауға болады

```
chown root:root /
systemd-tmpfiles --create
```

Қайталанатын зерттеулерден кейін, бұл github action tar `uid` пайдаланушысы `1001`

`--no-same-owner` параметрі болмаса , ол `1001` пайдаланушы үшін `uid` мәніне ашылады tar

Нәтижесінде rsync түбірлік каталогтың пайдаланушылар тобын өзгертеді, содан кейін қайта іске қосқаннан кейін ssh жүйесіне кіре алмайсыз.

Декомпрессия сценарийін келесідей өзгертіңіз:

```bash
tar --no-same-owner -xvf $TZT
rm $TZT

if [ -f "init.sh" ]; then
  ./init.sh
  rm init.sh
fi

rsync --remove-source-files -av . /
```