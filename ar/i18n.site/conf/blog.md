# قالب المدونة

في `i18n/conf.yml`، `use: Blog` يعني استخدام قالب المدونة للعرض.

يحتاج ملف `markdown` لمقالات المدونة إلى إعداد معلومات التعريف.

يجب أن تكون معلومات التعريف في بداية الملف، وتبدأ بـ `---` وتنتهي بـ `---`، وتكون صيغة معلومات الإعداد في المنتصف بلغة `YAML`.

configure file example is as follows:

```yml
---

brief: |
  this is a demo brief
  you can write multiline

---

# title

… …
```

`brief` يمثل ملخص المحتوى، وسيتم عرضه في صفحة فهرس المدونة.

بمساعدة `YMAL` '|`بناء الجملة، يمكنك كتابة ملخصات متعددة الأسطر.

إعداد شجرة المحتويات على الجانب الأيمن من المدونة هو نفسه ملف `TOC` (انظر الفصل السابق)، فقط المقالات المدرجة في `TOC` ستظهر في فهرس الصفحة الرئيسية للمدونة.

المقالات التي لا تحتوي على معلومات التعريف لن تظهر في الصفحة الرئيسية للمدونة، ولكنها قد تظهر في شجرة المحتويات على الجانب الأيمن.

إذا كنت ترغب في تثبيت مقال في القمة، قم أولاً بتشغيل `i18n.site` ثمحرر ملف `xxx.yml` الموجود في `.i18n/data/blog`، وقم بتغيير الطابع الزمني إلى عدد سالب (سيتم ترتيب الأعداد السالبة حسب القيمة المطلقة من الأصغر إلى الأكبر).