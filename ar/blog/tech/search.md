---

brief: |
  يدعم موقع i18n.site الآن البحث الكامل في النص بدون الحاجة إلى خادم.

  تقدم هذه المقالة وصفًا لتطبيق تقنية البحث الكامل في الواجهة الأمامية، وتتضمن الفهرس المقلوب الذي يُبنى باستخدام IndexedDB، والبحث المتقدم بالبادئات، وتحسين تقسيم الكلمات، ودعم متعدد اللغات.

  مقارنة بالحلول المتاحة حاليًا، يعتبر البحث الكامل في الواجهة الأمامية لموقع i18n.site صغيرًا في الحجم وسريعًا، مناسبًا للمواقع الصغيرة والمتوسطة مثل الوثائق والمدونات، ويمكن استخدامه دون اتصال بالإنترنت.

---

# البحث عن النص الكامل المقلوب للواجهة الأمامية النقية

## مقدمة

بعد عدة أسابيع من التطوير، [i18n.site](//i18n.site) (أداة إنشاء موقع ويب ثابتة تمامًا markdown multilingualtranslation & ) تدعم الآن البحث عن النص الكامل في الواجهة الأمامية.

<p style="display:flex;flex-wrap:wrap;justify-content:center"><img src="//p.3ti.site/1727600475.avif" style="width:320px"><img src="//p.3ti.site/1727602760.avif" style="width:320px"></p>

سأشارك في هذا المقال التنفيذ الفني للبحث عن النص الكامل للواجهة الأمامية في `i18n.site`، يمكنك تجربة نتائج البحث بزيارة [i18n.site](//i18n.site).

مفتوح المصدر: [نواة البحث](//github.com/i18n-site/ie/tree/main/qy) / [واجهة التفاعل](//github.com/i18n-site/plugin/tree/main/qy)

## نظرة عامة على حلول البحث عن النص الكامل بدون خادم

بالنسبة لمواقع الويب الثابتة البحتة الصغيرة والمتوسطة الحجم، مثل المستندات/المدونات الشخصية، يعد إنشاء واجهة خلفية للبحث عن النص الكامل أمرًا ثقيلًا جدًا، ويعتبر البحث عن النص الكامل بدون خدمة هو الخيار الأكثر شيوعًا.

تنقسم حلول البحث عن النص الكامل بدون خادم إلى فئتين رئيسيتين:

أولًا، هناك مقدمو خدمات البحث التابعون لجهات خارجية مثل [algolia.com](//algolia.com) الذين يقدمون مكونات أمامية للبحث عن النص الكامل.

تتطلب هذه الخدمات الدفع بناءً على حجم البحث، وغالبًا ما تكون غير متاحة للمستخدمين في الصين القارية بسبب مشكلات مثل امتثال موقع الويب.

لا يمكن استخدامه دون الاتصال بالإنترنت، ولا يمكن استخدامه على الإنترانت، وله قيود كبيرة. لن نناقش هذا النوع من الحلول في هذا المقال.

والثاني هو البحث عن النص الكامل للواجهة الأمامية.

في الوقت الحاضر، تشمل عمليات البحث عن النص الكامل الشائعة في الواجهة الأمامية [lunrjs](//lunrjs.com) و [ElasticLunr.js](//github.com/weixsong/elasticlunr.js) (تطوير ثانوي بناءً على `lunrjs`).

يقدم `lunrjs` طريقتين لبناء الفهارس، ولكل منهما مشكلاته الخاصة.

1. بناء الفهارس المسبق

   ولأن الفهارس تحتوي على كلمات من جميع المستندات، تكون كبيرة الحجم.
   عند إضافة مستند أو تعديله، يجب تحميل ملف فهرس جديد.
   سيؤدي ذلك إلى زيادة وقت انتظار المستخدم واستهلاك الكثير من النطاق الترددي.

2. بناء الفهارس عند تحميل المستندات

   بناء الفهارس مهمة مكثفة من الناحية الحسابية، وسيؤدي بناء الفهارس في كل مرة إلى تأخيرات واضحة وتجربة مستخدم سيئة.

---

بالإضافة إلى `lunrjs`، هناك بعض الحلول الأخرى للبحث عن النص الكامل، مثل:

[fusejs](//www.fusejs.io)، الذي يبحث عن التشابه بين السلاسل.

أداء هذا الحل ضعيف جدًا ولا يمكن استخدامه للبحث عن النص الكامل (انظر [Fuse.js يستغرق الاستعلام الطويل أكثر من 10 ثانية، وكيفية تحسينه؟](//stackoverflow.com/questions/70984437/fuse-js-takes-10-seconds-with-semi-long-queries) ).

[TinySearch](//github.com/tinysearch/tinysearch)، الذي يستخدم مرشح Bloom للبحث، ولا يمكن استخدامه للبحث عن البادئة (على سبيل المثال، أدخل `goo`، ابحث عن `good`، `google`)، ولا يمكن تحقيق تأثير إكمال تلقائي مماثل.

نظرًا لأوجه القصور في الحلول الحالية، قمنا بتطوير حل جديد للبحث عن النص الكامل للواجهة الأمامية في `i18n.site`، يتميز بالخصائص التالية:

1. يدعم البحث المتعدد اللغات وهو صغير الحجم، حجم نواة البحث بعد ضغطها بـ `gzip` هو `6.9KB` (كمقارنة، حجم `lunrjs` هو `25KB`)
1. يبنى الفهرس المقلوب بناءً على `indexedb`، مما يقلل من استهلاك الذاكرة ويجعلها سريعة
1. عند إضافة/تعديل المستندات، تتم إعادة فهرسة المستندات المضافة أو المعدلة فقط، مما يقلل من حجم العمليات الحسابية
1. يدعم البحث عن البادئة ويقوم بعرض نتائج البحث في الوقت الفعلي أثناء كتابة المستخدم
1. متاح حاليًا

سأقوم بتقديم تفاصيل التنفيذ الفني لـ `i18n.site` أدناه.

## تجزئة الكلمات متعددة اللغات

يستخدم تجزئة الكلمات واجهة تجزئة الكلمات الأصلية للمتصفح `Intl.Segmenter`، وتدعم جميع المتصفحات الرئيسية هذه الواجهة.

![](//p.3ti.site/1727667759.avif)

كود تجزئة الكلمة `coffeescript` هو كما يلي:

```coffee
SEG = new Intl.Segmenter 0, granularity: "word"

seg = (txt) =>
  r = []
  for {segment} from SEG.segment(txt)
    for i from segment.split('.')
      i = i.trim()
      if i and !'|`'.includes(i) and !/\p{P}/u.test(i)
        r.push i
  r

export default seg

export segqy = (q) =>
  seg q.toLocaleLowerCase()
```

في:

* `/\p{P}/` هو تعبير عادي يطابق علامات الترقيم وتشمل الرموز المطابقة المحددة ما يلي: `! " # $ % & ' ( ) * + , - . / : ; < = > ? @ [ \ ] ^ _ ` { | } `.</p><ul style=";text-align:right;direction:rtl"><li style=";text-align:right;direction:rtl"> `split('.')` لأن تجزئة كلمة المتصفح `Firefox` لا تقسم `.` .</li>


## بناء الفهرس

تم إنشاء خمس جداول لتخزين الكائنات في `IndexedDB`:

* `word` الكلمات : id -
* `doc` url - : id - رقم الإصدار
* `docWord` مجموعة من الوثيقة id - id :
* `prefix` مجموعة من البادئة - id :
* `rindex` : id - Document id : مجموعة من أرقام الأسطر

تم تمرير مصفوفة المستند `url` ورقم الإصدار `ver`، وتم البحث عما إذا كان المستند موجودًا في جدول `doc`، إذا لم يكن موجودًا، تم إنشاء فهرس مقلوب. وفي الوقت نفسه، تم إزالة الفهرس المقلوب لتلك المستندات التي لم يتم تمريرها.

بهذه الطريقة، يمكن تحقيق الفهرسة المتزايدة وتقليل مقدار الحسابات.

في التفاعل الأمامي، يمكن عرض شريط تقدم التحميل الخاص بالفهرس لتجنب التأخير عند التحميل لأول مرة، راجع "شريط التقدم مع الرسوم المتحركة، بناءً على تطبيق واحد progress + Pure css " [الإنجليزية](//dev.to/i18n-site/a-single-progress-uses-pure-css-to-achieve-animation-effects-2oo) / [الصينية](//juejin.cn/post/7413586285954154522).

### كتابة متزامنة عالية لـ IndexedDB

تم تطوير المشروع بناءً على التغليف غير المتزامن لـ IndexedDB [idb](//www.npmjs.com/package/idb).

عمليات القراءة والكتابة في IndexedDB غير متزامنة. عند إنشاء فهرس، يتم تحميل المستندات بشكل متزامن لإنشاء الفهرس.

لتجنب فقدان البيانات جزئيًا بسبب الكتابة التنافسية، يمكن الرجوع إلى الكود `coffeescript` أدناه وإضافة ذاكرة تخزين `ing` بين القراءة والكتابة لاعتراض عمليات الكتابة المنافسة.

```coffee
pusher = =>
  ing = new Map()
  (table, id, val)=>
    id_set = ing.get(id)
    if id_set
      id_set.add val
      return

    id_set = new Set([val])
    ing.set id, id_set
    pre = await table.get(id)
    li = pre?.li or []

    loop
      to_add = [...id_set]
      li.push(...to_add)
      await table.put({id,li})
      for i from to_add
        id_set.delete i
      if not id_set.size
        ing.delete id
        break
    return

rindexPush = pusher()
prefixPush = pusher()
```

## البحث عن البادئة في الوقت الحقيقي

من أجل عرض نتائج البحث أثناء كتابة المستخدم، على سبيل المثال، عند إدخال `wor`، يتم عرض الكلمات التي يسبقها `wor` مثل `words` و `work`.

![](//p.3ti.site/1727684944.avif)

ستستخدم نواة البحث جدول `prefix` للكلمة الأخيرة بعد تجزئة الكلمة للعثور على جميع الكلمات البادئة بها، والبحث بالتسلسل.

تُستخدم وظيفة مقاومة الاهتزاز `debounce` أيضًا في التفاعل الأمامي (يتم تنفيذها على النحو التالي) لتقليل تكرار إدخال المستخدم الذي يؤدي إلى عمليات البحث وتقليل مقدار العمليات الحسابية.

```js
export default (wait, func) => {
  var timeout;
  return function(...args) {
    clearTimeout(timeout);
    timeout = setTimeout(func.bind(this, ...args), wait);
  };
}
```

## الدقة والتذكير

سيقوم البحث أولاً بتقسيم الكلمات الرئيسية التي أدخلها المستخدم.

افترض أن هناك `N` كلمة بعد تجزئة الكلمة، عند إرجاع النتائج، سيتم إرجاع النتائج التي تحتوي على جميع الكلمات الرئيسية أولاً، ثم سيتم إرجاع النتائج التي تحتوي على `N-1`، `N-2`،...، `1` كلمة رئيسية.

تضمن نتائج البحث المعروضة أولاً دقة الاستعلام، والنتائج التي يتم تحميلها لاحقًا (انقر فوق الزر تحميل المزيد) تضمن معدل الاستدعاء.

![](//p.3ti.site/1727684564.avif)

## تحميل عند الطلب

من أجل تحسين سرعة الاستجابة، يستخدم البحث المولد `yield` لتنفيذ التحميل عند الطلب، ويعود `limit` مرة يتم فيها الاستعلام عن نتيجة.

لاحظ أنه في كل مرة تقوم فيها بالبحث مرة أخرى بعد `yield`، فإنك تحتاج إلى إعادة فتح معاملة استعلام بقيمة `IndexedDB`.

## البحث عن البادئة في الوقت الحقيقي

من أجل عرض نتائج البحث أثناء كتابة المستخدم، على سبيل المثال، عند إدخال `wor`، يتم عرض الكلمات التي يسبقها `wor` مثل `words` و `work`.

![](//p.3ti.site/1727684944.avif)

ستستخدم نواة البحث جدول `prefix` للكلمة الأخيرة بعد تجزئة الكلمة للعثور على جميع الكلمات البادئة بها، والبحث بالتسلسل.

تُستخدم وظيفة مقاومة الاهتزاز `debounce` أيضًا في التفاعل الأمامي (يتم تنفيذها على النحو التالي) لتقليل تكرار إدخال المستخدم الذي يؤدي إلى عمليات البحث وتقليل مقدار العمليات الحسابية.

```js
export default (wait, func) => {
  var timeout;
  return function(...args) {
    clearTimeout(timeout);
    timeout = setTimeout(func.bind(this, ...args), wait);
  };
}
```

## متاح حاليًا

لا يقوم جدول الفهرس بتخزين النص الأصلي، بل الكلمات فقط، مما يقلل من حجم التخزين.

يتطلب تمييز نتائج البحث إعادة تحميل النص الأصلي، كما أن مطابقة `service worker` يمكن أن تتجنب طلبات الشبكة المتكررة.

في الوقت نفسه، نظرًا لأن `service worker` يقوم بتخزين جميع المقالات مؤقتًا، بمجرد قيام المستخدم بإجراء بحث، يصبح موقع الويب بالكامل، بما في ذلك البحث، متاحًا دون اتصال بالإنترنت.

## تحسين عرض المستندات MarkDown

تم تحسين حل البحث الأمامي الخالص الخاص بـ `i18n.site` لمستندات `MarkDown`.

عند عرض نتائج البحث، سيتم عرض اسم الفصل وسيتم التنقل في الفصل عند النقر عليه.

![](//p.3ti.site/1727686552.avif)

## الخاتمة

يتم تطبيق البحث عن النص الكامل المقلوب على الواجهة الأمامية فقط، دون الحاجة إلى خادم. مناسب جدًا للمواقع الصغيرة والمتوسطة الحجم مثل المستندات والمدونات الشخصية.

`i18n.site` هو بحث أمامي نقي مفتوح المصدر تم تطويره ذاتيًا، صغير الحجم وسريع الاستجابة، يحل عيوب البحث عن النص الكامل الحالي للواجهة الأمامية النقية ويوفر تجربة مستخدم أفضل.