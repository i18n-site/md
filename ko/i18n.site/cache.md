# 프런트엔드 캐시

## 프런트엔드 캐시 업데이트 지연

[MDN](https://developer.mozilla.org/docs/Web/API/Service_Worker_API) 에 따르면 `service worker` 의 최대 업데이트 빈도는 24 시간입니다.

i18n.site 에 CDN 이름을 캐시하세요 `service worker` 따라서 구성에서 `cdn` 수정되면 기존 사용자는 업데이트를 보려면 24 시간을 기다려야 합니다.

## 로컬 디버깅에서 프런트 엔드 캐시를 지우는 방법

TODO 크롬
