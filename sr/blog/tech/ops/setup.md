# Иницијализујте Сервер (Користите БТРФС Партицију, Снаппер Планирани Снимак)

## Цонтабо Сервер Куповине

цонтабо је јефтин ВПС провајдер у Немачкој са дугом историјом (више од 20 година) и много корисника (више од 200.000), тако да нема потребе да бринете о бежању.

VPS купујете [contabo.com](//contabo.com) не заборавите да изаберете евро цене, које су јефтиније.

<img alt="" src="https://i-01.eu.org/1718285782.avif">

Постоји једнократна накнада за инсталацију за цонтабо.
Међутим, постоји неколико дана сваког месеца када је накнада за инсталацију бесплатна. Само се региструјте и обратите пажњу на промотивне е-поруке.

Поред накнаде за слободну површину у Европи, други региони имају различите нивое повећања цена.

Одабрао сам европски сервер. У сваком случају, након коришћења цлоудфлареа, нема велике разлике између сервера.

Коначна конфигурација је следећа, 16 ГБ меморије, 6 ЦПУ, 200 ГБ НВМе, 9,50 евра месечно, што одговара 74,43 РМБ.

<img alt="" src="https://i-01.eu.org/1718286008.avif">

**Ова цена је скоро једна десетина цене Алибаба Цлоуд-а и Амазон-а** .

<img alt="" src="https://i-01.eu.org/1718356956.avif">

Користио [Yet-Another-Bench-Script](https://github.com/masonr/yet-another-bench-script) за покретање перформанси и параметара:

```
curl -sL yabs.sh | bash -s -- -s https://www.vpsbenchmarks.com/yabs/upload
```

Приказ модела CPU 1285 перформанси са једним језгром `AMD EPYC 9224 24-Core Processor`
, вишејезгарни резултат 4277 Погледајте још резултата [vpsbenchmarks.com](https://www.vpsbenchmarks.com/yabs/contabo-6c-16gb-3fb7d8)

Купио сам три сервера у исто време и активирао их у исто време. Ово обезбеђује да се сервери налазе у истој рачунарској соби, што олакшава изградњу високе доступности у будућности.

Његови сервери повремено прекидају везу, обезбеђујући приступачност веб локацији кроз високу доступност.

Нови корисници приликом куповине морају да доставе своје личне карте ради провере аутентичности са правим именом.

## Конфигуришите Пријављивање Без Лозинке

Након неколико минута, сви сервери су били активирани.

Прва ствар је да уредите псеудоним за `~/.ssh/config` IP а затим користите `ssh-copy-id root@c2` да бисте конфигурисали пријаву без лозинке.

<img alt="" src="https://i-01.eu.org/1718287198.avif">

Можете да измените ову [ssh-copy-id.sh](https://github.com/i18n-ops/ubuntu/blob/main/ssh-copy-id.sh) да бисте брже завршили конфигурацију иницијализације.

Након што је конфигурација без лозинке завршена, можете је користити у групама, што је згодно [pdsh](https://github.com/chaos/pdsh)

Први је да ручно омогућите ипв6 и тестирате

```bash
pdsh -w 'c0 c1 c2' 'bash -i -c enable_ipv6'
pdsh -w 'c0 c1 c2' 'curl -s 6.ipw.cn;echo'
```

<img alt="" src="https://i-01.eu.org/1718287969.avif">

Затим надоградите систем на убунту 24 једним кликом и инсталирајте софтвер који се обично користи.

```bash
pdsh -w 'c0 c1 c2' 'curl -s https://raw.githubusercontent.com/i18n-ops/ubuntu/main/boot.sh | CN=1 bash'
```

ЦН=1 значи пребацивање језика на кинески и временске зоне на Пекинг.

Софтвер који је подразумевано инсталиран је конфигурисан у складу са мојим личним навикама коришћења, као што су [mise](https://github.com/jdx/mise) [direnv](https://direnv.net/) , итд. Ако је потребно, можете копирати [i18n-ops/ubuntu](https://github.com/i18n-ops/ubuntu) варехоусе и изменити га по потреби!

Овај корак траје дуго Након чекања од пола сата, надоградња и инсталација често коришћеног софтвера ће бити успешна. Можете то потврдити следећом командом:

```bash
pdsh -w 'c0 c1 c2' 'cat /etc/issue'
```

<img alt="" src="https://i-01.eu.org/1718290346.avif">

## Мосх Убрзава Ссх

Мрежа од Европе до Кине није стабилна `ssh` Препоручљиво је користити [mosh](//mosh.org)

Креирајте скрипту `c0` у `~/.bin` локалне машине (углавном додатој променљивој окружења `PATH` ) на следећи начин:

```bash
#!/уср/бин/енв басх

if [ -z "$1" ]; then
arg=/root/.tmux_default
else
arg=${@:1:$#}
fi

vps=$(basename $0)
set -ex
mosh $vps -p 60000:60009 $arg
```

Након креирања, доделите извршне дозволе и софт-линк `c1` `c2` !

```bash
chmod +x c0
ln -s c0 c1
ln -s c0 c2
```

У будућности можете директно да унесете `c0` `c1` и можете да користите директну везу `mosh` брзине и унесите [tmux](https://www.ruanyifeng.com/blog/2019/10/tmux.html)

## Конвертујте Формат Партиције Из Ект4 У Бтрфс

Може [btrfs](https://wikipedia.org/wiki/Btrfs) прави снимке диска ради лакшег прављења резервних копија.

Прво уђите у систем спасавања

<img alt="" src="https://i-01.eu.org/1718300448.avif">

Изаберите Дебиан Ресцуе

<img alt="" src="https://i-01.eu.org/1718300753.avif">

Сачекајте да се сервер поново покрене

Промене отиска прста ће се десити када ssh користите `ssh-keygen -R ip`

Затим покрените следећу скрипту да бисте конвертовали партицију

```bash
nohup bash -c "bash <(curl -s https://raw.githubusercontent.com/i18n-ops/ops/main/contabo/ext4_btrfs.sh) >/tmp/nohup.out 2>&1 && reboot" &
sleep 1 && tail -f /tmp/nohup.out
```

Скрипта ће аутоматски поново покренути систем након што конверзија буде успешна.

### Погледајте Компресију Бтрфс

Подразумевано ће се аутоматски компримовати Команда за проверу компресије је следећа:

```bash
if ! command -v compsize &>/dev/null; then
apt install -y  btrfs-compsize
fi
compsize -x /
```

### Ручна Компресија

Ако сте перфекциониста, можете и ручно извршити комплетну компресију диска

```bash
btrfs filesystem defragment -r -czstd /
```

Као што је приказано на слици испод, можете видети да је након ручне компресије у новом систему компримовано још скоро 100МБ.

<img alt="" src="https://i-01.eu.org/2023/12/8NWlr4c.webp">

### Направите Резервну Копију Бтрфс Снимака Помоћу Снаппер-А

[snapper](https://manual.siduction.org/sys-admin-btrfs-snapper_en.html) је најбољи партнер за Btrfs бацкуп.

Скрипта за иницијализацију ће креирати `~/ubuntu` , само покрените следећу команду:

```
~/ubuntu/snapper/init.sh
```

Видимо се у садржају сценарија [github.com/i18n-ops/ubuntu/blob/main/snapper/init.sh](https://github.com/i18n-ops/ubuntu/blob/main/snapper/init.sh)

Користите `snapper list-configs`

<img alt="" src="https://i-01.eu.org/1718302689.avif">

Користите `snapper -c mnt list`

`/etc/snapper/configs/mnt` Можете погледати специфичне поставке резервне копије овог подволума

Више референци:

[Лако се играјте са функцијом снимања Btrfs snapper](https://zhuanlan.zhihu.com/p/31082518)

## Цонтабо Снимак Сервера

contabo функцију снимања сервера, али не подржава планиране снимке. Написао сам [github action](https://github.com/i18n-cron/cron/actions/workflows/contabo.snapshot.yml) за снимање дневних снимака.

<img alt="" src="https://i-01.eu.org/1718304206.avif">

Да бисте вратили снимак сервера, прво кликните на Цронтол у менију за управљање

<img alt="" src="https://i-01.eu.org/1718304275.avif">

Затим кликните на Снимци за враћање.

<img alt="" src="https://i-01.eu.org/1718304063.avif">

Међутим, грануларност овог снимка је превелика, када се врати, новододати подаци ће бити потпуно изгубљени, па га користите опрезно.

Обично је згодније користити бтрфс снимке.

## Детаљи О Систему Спасавања

Уђите у систем за 22 Након што је порт [tcping](https://github.com/paradiseduo/tcping) , нећете моћи да му приступите са постављеном лозинком 2-3 минута.

```
tcping ip 22 -c 9999
```

Приступачност порта се може пратити.

### Како Приступити Чврстом Диску Након Уласка У Ресторе Снапсхот &

```
mkdir -p /mnt/os
mount  /dev/sda3 /mnt/os -t btrfs -o "defaults,ssd,discard,noatime,compress=zstd:3,space_cache=v2"
mount --bind /dev /mnt/os/dev
mount --bind /proc /mnt/os/proc
mount --bind /sys /mnt/os/sys
mount --bind /run /mnt/os/run
chroot /mnt/os zsh -c "mount -a && dbus-daemon --system 2>/dev/null; exec zsh"
```

Можете погледати снимак, на пример

```
snapper -c etc list
```

Прегледајте измене датотеке снимка

```
snapper -c etc status 5..6
```

Погледајте одређени текст разлике

```
snapper -c etc diff 3..5
```

Врати снимак: На пример, поништите све промене након снимка 5

```
snapper -c etc undochange 1..0
snapper -c disk undochange 1..0
```

### Омогућите Постојаност Дневника

Омогућавање постојаности евиденције у систему спасавања захтева да ручно креирате одговарајући директоријум и извршите неке додатне кораке, јер можда немате покренут системд сервис. Ево корака које можете испробати:

Прво цхроот, онда

```
mkdir -p /var/log/journal
chown root:systemd-journal /var/log/journal
chmod 2755 /var/log/journal
sed -i 's/^#\?\s*Storage=.*/Storage=persistent/' /etc/systemd/journald.conf
```

Поново покрените свој Убунту систем: Након што извршите горенаведене измене, потребно је да поново покренете машину нормално и у свој Убунту систем уместо у систем за спасавање.

На овај начин, системд-јоурналд ће почети са новом конфигурацијом и почети да стално складишти евиденције на чврстом диску.

Имајте на уму да се горе наведене операције изводе на систему за спасавање, тако да ће те промене ступити на снагу тек када се систем поново покрене и више неће бити у режиму спасавања.

То значи да нећете одмах видети да су промене у постојећим евиденцијама постале трајне, али ће сви дневники генерисани након овога бити трајни.

На овај начин можете прегледати евиденцију ссх услуге

```
journalctl --directory=/var/log/journal  _SYSTEMD_UNIT=ssh.service
```

## Напомене О Тешким И Компликованим Болестима

### Решите Чудан Проблем: Сервер Не Може Ссх

Раније сам користио гитхуб акцију да направим аутоматску компилацију и скрипту за примену, али сам открио да услуга постављања не може бити ссх.

Улазак у систем спасавања и гледање дневника је проблем са дозволом:

`Missing privilege separation directory: /run/sshd`

Можете креирати овај директоријум помоћу следеће команде

```
chown root:root /
systemd-tmpfiles --create
```

Након поновљених истраживања, то је зато github action је корисник који је поставио `1001` tar `uid`

Распакивање мора бити обављено без параметара tar биће декомпримовано у `uid` за корисника `1001` `--no-same-owner`

Као резултат тога, рсинц ће променити корисничку групу основног директоријума и тада нећете моћи да се пријавите на ссх након поновног покретања.

Измените скрипту за декомпресију на следећи начин:

```bash
tar --no-same-owner -xvf $TZT
rm $TZT

if [ -f "init.sh" ]; then
  ./init.sh
  rm init.sh
fi

rsync --remove-source-files -av . /
```

